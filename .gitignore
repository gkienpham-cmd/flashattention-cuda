# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# CUDA / cpp_extension JIT build artifacts (compiled per-session on Colab; never committed)
build/
*.so
*.o
*.ninja*
.cache/
torch_extensions/

# Bench outputs that are machine-specific (the curated rows live in docs/results.md by hand)
bench/raw/
*.nsys-rep
*.ncu-rep

# OS / editor / agent cruft
.DS_Store
.vscode/
# Claude Code local settings + agent worktrees (15 MB of checkouts) — never commit
.claude/

# Large source papers (cited by arXiv id in docs/)
*.pdf
# Exception: papers/ directory holds the PMBS@SC related work references
!papers/*.pdf

# Raw research dumps — the curated synthesis lives in docs/{b300-decode,fa-next}-research.md
/B300 FlashAttention Optimization Research.txt

# Redundant raw Colab exports — the curated run-of-record notebooks live in notebooks/
/step5-output.ipynb
/v6 outputs.ipynb
/v6_decode_gate_outputs.ipynb

# Screenshots dropped at the repo root
/Screenshot*.png
