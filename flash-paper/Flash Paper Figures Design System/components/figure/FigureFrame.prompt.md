White figure canvas with the house-style 22px bold title and optional gray subtitle — start every HTML figure draft with it before exporting to SVG.

```jsx
<FigureFrame title="Each engine sets its own ridge" subtitle="MLA decode AI = 835 FLOP/byte" width="column">
  …figure content…
</FigureFrame>
```

`width`: `'column'` (600px, ~3.5in) | `'full'` (760px, ~7.2in) | number.
